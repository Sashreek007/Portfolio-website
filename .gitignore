node_modules
.astro
.vercel
.dist
/dist
.env
.env.*

# macOS
.DS_Store
.env*.local

# Personal docs — not for version control
sashreek_personality.md
sashreek_portfolio_theme.md
.next
tsconfig.tsbuildinfo

# iCloud sync exclusions (symlinked real dirs)
*.nosync

# Vendored CC0 source assets (Blender Studio Human Base Meshes, 48MB) —
# fetched by scripts/blender/fetch-human-base.sh, not tracked.
assets/vendor/
